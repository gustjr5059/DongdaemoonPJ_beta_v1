rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read,write: if true;
      // allow write: if request.auth != null; // 쓰기 권한은 인증된 사용자에게만 부여
    }
  }
}
